# 复制为 .env 后填写真实值（.env 已被 git 忽略，不会提交）
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=ps_web_user
DB_PASSWORD=在这里填数据库密码
DB_NAME=ps_web

# tushare 数据源 token（https://tushare.pro 个人主页获取）
TUSHARE_TOKEN=在这里填你的tushare token

# 仅初始化建库时使用：运行 scripts/init_db.py 需要，建完建议删除这一行
MYSQL_ROOT_PASSWORD=在这里临时填root密码
